# Update ArchMarshal in Codex

Copy the complete prompt below into Codex. This is the dedicated update
command. The idempotent prompt in `INSTALL_PROMPT.md` remains valid for both a
first installation and an update.

<!-- BEGIN UPDATE PROMPT -->
```text
Update the installed ArchMarshal management plugin to the latest verified version from https://github.com/yptang98/ArchMarshal. If ArchMarshal is not installed, safely perform a first installation instead, using the same verification and rollback rules.

This is a Codex plugin-state operation, not a project-governance operation. Do not run ArchMarshal against the current project. Do not clone into the current project, create a virtual environment there, save plans there, or modify any project or Skill file. Changes are limited to Codex-managed ArchMarshal marketplace/plugin state and ArchMarshal-specific backup or isolated-runtime directories under CODEX_HOME.

Complete the work yourself under these safety constraints; do not hand the steps back to me:

1. Confirm that `codex plugin`, Git, and Python 3.10-3.13 are available. Reuse existing Git/GitHub authentication. Never request, print, copy, or write tokens, passwords, cookies, SSH private keys, or the complete Codex configuration.
2. Resolve the remote default branch HEAD to a full 40-character commit SHA and confirm that GitHub Actions CI succeeded for that exact SHA. Use only that immutable SHA; do not update to an unpinned branch.
3. Inspect `codex plugin marketplace list --json` and `codex plugin list --available --json`. Require the unique marketplace name `archmarshal`, plugin identity `archmarshal@archmarshal`, and repository origin above. Stop without changing anything on ambiguous origin, duplicate identity, or an unexpected same-named marketplace.
4. Record the installed state. If the exact SHA and version are already installed and enabled, make no redundant change and report a verified no-op. If no prior installation exists, use the first-install path from `INSTALL_PROMPT.md`. If the marketplace is a user-owned local checkout, never delete, move, reset, pull, or rewrite it; report that automatic replacement is refused.
5. Keep the installed version enabled while preparing the replacement. Fetch the candidate exact SHA into a commit-scoped directory below `CODEX_HOME/updates/archmarshal/`, never the current project. Run the candidate checkout's `plugins/archmarshal/scripts/run_archmarshal.py --bootstrap-status` directly. Run read-only `doctor` against a nonexistent system-temporary path by invoking candidate `invoke_archmarshal.py` directly with the active Python and `-I`; do not temporarily replace `current.json`. Require `mode=ready`, `verified=true`, `marketplace=archmarshal`, `dependency_imported=false`, matching plugin/engine versions, and a successful doctor result before any plugin or marketplace removal. A stale or malformed runtime pointer must not block the dependency-free candidate bootstrap check.
6. Use the active Python as the lightweight fast path. Only if candidate doctor reports missing dependencies, prepare a commit-scoped isolated runtime below `CODEX_HOME/runtimes/archmarshal/` without publishing it as current. Read dependencies only from the pinned `pyproject.toml`; install only its wheel dependency closure, retain the pip report, run `pip check`, and use the copied non-symlinked interpreter with `-I` against candidate `invoke_archmarshal.py`. Never modify system Python, install ArchMarshal itself as an ambient package, accept arbitrary URLs, or build untrusted source dependencies. If preparation fails, stop with the old version untouched and usable.
7. After candidate validation, use the candidate's stdlib-only `plugins/archmarshal/scripts/update_support.py create` command to create a small UTC-timestamped last-known-good capsule below `CODEX_HOME/backups/archmarshal/`; do not assemble it with ad hoc copy commands. Record the old repository identity, full SHA, version, runtime pointer, related paths, and exact rollback commands. In repository-relative layout, the helper preserves `.agents/plugins/marketplace.json`, the old `plugins/archmarshal/` directory, its locked `src/archmarshal/` engine source, and `pyproject.toml`, and publishes `CAPSULE.json` last with a hash for every file. Run `update_support.py verify`, verify the capsule launcher and local-marketplace recovery path, and retain old commit-scoped runtimes. Do not copy marketplace history, credentials, or the complete Codex configuration. Do not remove the active old plugin or marketplace until both candidate and fallback verification succeed.
8. Perform the shortest possible cutover through `codex plugin` commands only: unregister the old `archmarshal@archmarshal` plugin and Codex-managed marketplace, immediately re-add `yptang98/ArchMarshal` pinned with `--ref` to the verified full SHA, then add `archmarshal@archmarshal`. Do not directly delete plugin caches or edit Codex configuration files. Atomically publish a prepared `archmarshal-runtime-v1` pointer only after the new plugin is installed. If no isolated runtime is needed, leave an absent pointer absent and retain a valid old-version pointer for rollback; the new launcher will ignore it. Preserve and atomically quarantine a malformed pointer during cutover. Require the installed plugin to be enabled and repeat bootstrap plus read-only doctor through its launcher. If any cutover or validation step fails, atomically restore the old runtime pointer and restore the last known-good pinned plugin/marketplace immediately. If the old Git pin cannot be restored, register the verified capsule as a temporary local recovery marketplace and reinstall the old plugin from it; report this recovery mode explicitly. Keep the failed candidate isolated for diagnosis.
9. Report whether the result was installed, updated, unchanged, refused, or restored; include old/new full SHAs and versions, capsule location, runtime fast path or isolated-runtime choice, bootstrap verification, and doctor result. The current task may finish with its already loaded old Skill instructions; after a version change, start a new Codex task to load the new version.
```
<!-- END UPDATE PROMPT -->
